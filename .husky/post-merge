#!/usr/bin/env sh

if git rev-parse --verify ORIG_HEAD >/dev/null 2>&1 &&
  git diff --name-only ORIG_HEAD HEAD -- package-lock.json | grep -q '^package-lock.json$'; then
  echo "package-lock.json changed; running npm install"
  npm install
fi
